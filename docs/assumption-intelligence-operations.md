# Assumption intelligence operations

Back up the SQLite database and the local `market-data` directory together. Periodically run the integrity endpoint, an offline backup/restore drill, the cost guard, and the full test suite. Preserve imported originals, snapshots, observations, runs, evidence, artifacts, decisions, and exports according to institutional retention policy. If source bytes or hashes do not match, quarantine the dataset and do not rely on the recommendation.
