[project]
name = "test3-cre-diligence"
version = "0.1.0"
description = "Local-first CRE deal intake and due diligence engine"
requires-python = ">=3.11"
license = {text = "MIT"}

[project.scripts]
test3 = "test3.api:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

